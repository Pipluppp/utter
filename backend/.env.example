# Environment
ENV=development

# Database (SQLite for local dev)
DATABASE_URL=sqlite+aiosqlite:///./utter.db

# Upload directory
UPLOAD_DIR=./uploads

# TTS Provider (only "qwen" supported)
TTS_PROVIDER=qwen
