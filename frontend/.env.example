## Supabase (required for Auth)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

## Local dev convenience (FastAPI parity)
## If set, frontend sends `x-utter-user-id` to the local `/api/*` backend.
## In production (Vercel → Supabase Edge), this header is not used for auth.
VITE_DEBUG_USER_ID=

